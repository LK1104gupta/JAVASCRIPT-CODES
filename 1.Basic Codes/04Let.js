// Let Keyword
// Declare variable with Let keyword
let Naam = "Lalit";
console.log(Naam);
// let Naam = "Sagar"; Error
Naam = "Sagar"
console.log(Naam);

// Let was introduced First in ES6, before that only var was in picture, there were some problem with
// var to overcome those problems Let was introduced. 
// Block scope vs function scope(reason for using let instead of var)